/**
 * Application constants
 */

export const INTERVAL_TIMES = {
  TIME_UPDATE: 60000, // Update time every minute (only shows hour:minute)
  DATE_CHECK: 60000 // Check date change every minute
} as const

export const MESSAGE_TIMEOUT = 3200 // milliseconds

export const STATISTICS_DAYS = 30

export const ATTENDANCE_HISTORY_LIMIT = 10

